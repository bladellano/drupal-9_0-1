<?php

/**
 * Implements hook_theme().
 *
 * We use hook_theme to define the templates used.
 */
function points_on_xingu_maps_theme($existing, $type, $theme, $path)
{
  return [
    'basic_map_block' => [
      'variables' => [
        'text' => '',
        'items' => [],
      ],
    ]
  ];
}
